name: .NET

on:
  push:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x

    - name: Create Local NuGet Directory
      run: mkdir ~/nuget

    - name: Restore Infra.SqlApi
      run: dotnet restore ./*/Infra.SqlApi.slnx

    - name: Build Infra.SqlApi
      run: dotnet build ./*/Infra.SqlApi.slnx --no-restore -c Release

    - name: Test Infra.SqlApi
      run: dotnet test ./*/Infra.SqlApi.slnx --no-restore -c Release

    - name: Pack Infra.SqlApi
      run: dotnet pack ./*/Infra.SqlApi.slnx --no-build -o ~/nuget -c Release

    - name: Push Packages
      if: ${{ github.ref == 'refs/heads/main' }}
      run: >
        dotnet nuget push ~/nuget/*.nupkg
        -s https://api.nuget.org/v3/index.json
        -k ${{ secrets.NUGET_SECRET }}
        --skip-duplicate